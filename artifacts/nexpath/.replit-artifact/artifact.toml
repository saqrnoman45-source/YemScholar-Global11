kind = "web"
previewPath = "/"
title = "NexPath"
version = "1.0.0"
id = "artifacts/nexpath"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22057

[services.development]
run = "pnpm --filter @workspace/nexpath run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/nexpath", "run", "build" ]
publicDir = "artifacts/nexpath/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22057"
BASE_PATH = "/"
